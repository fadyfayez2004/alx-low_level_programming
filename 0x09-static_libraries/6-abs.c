#include "main.h"

/**
  * _abs - find the absolute value of n
  * @n: the nubmer to be modified
  * Return: n if positve, -n if negative
  */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
